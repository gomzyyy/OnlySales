import { Product } from '../../types'

export type useProductDetailsReturnType={
b:number
}

const useProductDetails = (product:Product):useProductDetailsReturnType => {
 
const x = product
 
  return {
b:8
  }
}

export default useProductDetails